#summary Description of the application layers.

SocialConnect is separated in two layers:


The Model layer has the model for platforms, accounts and deals with authentification. When the model layer validates a request it send it to the proxy layer whow send the request to the remote API.


== Platforms and Accounts ==